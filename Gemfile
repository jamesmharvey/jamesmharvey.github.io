# For local preview: `bundle install && bundle exec jekyll serve`
# GitHub Pages builds the site server-side, so this is only for local work.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
